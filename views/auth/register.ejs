<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Store - Registration</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css"
        integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        <link href="/css/navbar.css" rel="stylesheet">
    <link href="/css/footer.css" rel="stylesheet">
    
    <style>
        /* Modern background styling */
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            height: auto;
            position: relative;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        /* Remove overlay */
        body::before {
            display: none;
        }

        /* Container adjustments */
        .container.mt-4 {
            position: relative;
            z-index: 1;
            padding: 40px 0;
            height: auto;
        }

        /* Main content layout */
        .main-content {
            display: flex;
            min-height: 80vh;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
        }

        /* Left side - Hero section */
        .hero-section {
            flex: 1;
            background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
            color: white;
            padding: 50px 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('https://images.unsplash.com/photo-1551288049-bebda4e38f71?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=870&q=80') center/cover;
            opacity: 0.2;
        }

        .hero-content {
            position: relative;
            z-index: 1;
        }

        .hero-title {
            font-size: 2.8rem;
            font-weight: 800;
            margin-bottom: 1.5rem;
            line-height: 1.2;
        }

        .hero-subtitle {
            font-size: 1.3rem;
            margin-bottom: 2rem;
            opacity: 0.9;
            line-height: 1.6;
        }

        .feature-list {
            list-style: none;
            padding: 0;
            margin-bottom: 3rem;
        }

        .feature-list li {
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            font-size: 1.1rem;
        }

        .feature-list i {
            margin-right: 12px;
            color: #4cd964;
            font-size: 1.2rem;
        }

        .testimonial {
            background: rgba(255, 255, 255, 0.1);
            padding: 25px;
            border-radius: 12px;
            border-left: 4px solid #4cd964;
            margin-top: 2rem;
        }

        .testimonial-text {
            font-style: italic;
            margin-bottom: 1rem;
            line-height: 1.6;
        }

        .testimonial-author {
            font-weight: 600;
            color: #4cd964;
        }

        /* Right side - Form section */
        .form-section {
            flex: 1;
            background: white;
            padding: 40px 35px;
            /* display: flex; */
            flex-direction: column;
            justify-content: center;
            position: relative;
            overflow: scroll;
        }

        .form-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('https://images.unsplash.com/photo-1556742049-0cfed4f6a45d?ixlib=rb-1.2.1&auto=format&fit=crop&w=1950&q=80') center/cover;
            opacity: 0.05;
            z-index: 0;
        }

        .form-content {
            position: relative;
            z-index: 1;
        }

        .form-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .form-title {
            font-size: 1.8rem;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 0.5rem;
        }

        .form-subtitle {
            color: #7f8c8d;
            font-size: 1rem;
        }

        /* Form styling */
        .form-group {
            margin-bottom: 1.5rem;
            position: relative;
        }

        .form-label {
            font-weight: 600;
            margin-bottom: 8px;
            color: #2c3e50;
            font-size: 0.95rem;
        }

        .form-control {
            border-radius: 8px;
            border: 1.5px solid #e1e5e9;
            padding: 12px 16px;
            font-size: 15px;
            transition: all 0.3s ease;
            background: #fafbfc;
            width: 100%;
        }

        .form-control:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.1rem rgba(102, 126, 234, 0.15);
            background: white;
        }

        .form-control::placeholder {
            color: #9ca3af;
            font-size: 14px;
        }

        /* Gender radio buttons - simplified */
        .gender-group {
            display: flex;
            gap: 15px;
            margin-top: 5px;
        }

        .gender-option {
            flex: 1;
        }

        .gender-option input[type="radio"] {
            display: none;
        }

        .gender-label {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 10px 15px;
            border: 1.5px solid #e1e5e9;
            border-radius: 8px;
            background: #fafbfc;
            cursor: pointer;
            transition: all 0.3s ease;
            font-weight: 500;
            font-size: 14px;
            text-align: center;
        }

        .gender-option input[type="radio"]:checked+.gender-label {
            border-color: #667eea;
            background: rgba(102, 126, 234, 0.08);
            color: #667eea;
        }

        /* File upload styling */
        .file-upload-group {
            border: 1.5px dashed #e1e5e9;
            border-radius: 8px;
            padding: 15px;
            background: #fafbfc;
            text-align: center;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .file-upload-group:hover {
            border-color: #667eea;
            background: rgba(102, 126, 234, 0.05);
        }

        .file-upload-label {
            display: flex;
            flex-direction: column;
            align-items: center;
            color: #667eea;
            font-weight: 500;
            cursor: pointer;
            margin: 0;
        }

        .file-upload-label i {
            font-size: 24px;
            margin-bottom: 8px;
        }

        .file-upload-text {
            font-size: 14px;
            color: #6b7280;
        }

        .custom-file-input {
            display: none;
        }

        /* Button styling */
        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            border-radius: 8px;
            padding: 14px 30px;
            font-weight: 600;
            font-size: 16px;
            width: 100%;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            margin-top: 1rem;
        }

        .btn-primary:hover {
            transform: translateY(-1px);
            box-shadow: 0 8px 20px rgba(102, 126, 234, 0.3);
        }

        .btn-text {
            transition: opacity 0.3s;
        }

        .btn-loading .btn-text {
            opacity: 0;
        }

        .loading-spinner {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 20px;
            height: 20px;
            border: 2px solid transparent;
            border-top: 2px solid white;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            opacity: 0;
            transition: opacity 0.3s;
        }

        .btn-loading .loading-spinner {
            opacity: 1;
        }

        @keyframes spin {
            0% {
                transform: translate(-50%, -50%) rotate(0deg);
            }

            100% {
                transform: translate(-50%, -50%) rotate(360deg);
            }
        }

        /* Login link */
        .login-link {
            text-align: center;
            margin-top: 1.5rem;
            color: #7f8c8d;
            font-size: 14px;
        }

        .login-link a {
            color: #667eea;
            font-weight: 600;
            text-decoration: none;
        }

        .login-link a:hover {
            text-decoration: underline;
        }

        /* Form row for better spacing */
        .form-row {
            display: flex;
            gap: 15px;
            margin-bottom: 1.5rem;
        }

        .form-row .form-group {
            flex: 1;
            margin-bottom: 0;
        }

        /* ========== RESPONSIVE DESIGN ========== */

        /* XS Screen (Extra Small) - Mobile Portrait */
        @media (max-width: 575.98px) {
            .container.mt-4 {
                margin-top: 1rem !important;
                padding: 10px;
            }

            .main-content {
                flex-direction: column;
                border-radius: 15px;
            }

            .hero-section {
                padding: 30px 20px;
                border-radius: 15px 15px 0 0;
            }

            .hero-title {
                font-size: 2rem;
            }

            .hero-subtitle {
                font-size: 1.1rem;
            }

            .form-section {
                padding: 25px 20px;
                border-radius: 0 0 15px 15px;
            }

            .form-title {
                font-size: 1.5rem;
            }

            .gender-group {
                flex-direction: column;
                gap: 10px;
            }

            .form-row {
                flex-direction: column;
                gap: 1rem;
            }
        }

        /* SM Screen (Small) - Mobile Landscape */
        @media (min-width: 576px) and (max-width: 767.98px) {
            .container.mt-4 {
                margin-top: 2rem !important;
                padding: 20px;
            }

            .main-content {
                flex-direction: column;
            }

            .hero-section {
                padding: 40px 30px;
            }

            .form-section {
                padding: 35px 30px;
            }
        }

        /* MD Screen (Medium) - Tablets */
        @media (min-width: 768px) and (max-width: 991.98px) {
            .container.mt-4 {
                margin-top: 3rem !important;
            }

            .hero-section {
                padding: 40px 30px;
            }

            .form-section {
                padding: 40px 35px;
            }

            .hero-title {
                font-size: 2.4rem;
            }
        }

        /* LG Screen (Large) - Desktops */
        @media (min-width: 992px) and (max-width: 1199.98px) {
            .container.mt-4 {
                margin-top: 4rem !important;
            }

            .hero-section {
                padding: 50px 40px;
            }

            .form-section {
                padding: 45px 40px;
            }
        }

        /* XL Screen (Extra Large) - Large Desktops */
        @media (min-width: 1200px) and (max-width: 1399.98px) {
            .container.mt-4 {
                margin-top: 5rem !important;
                max-width: 1400px;
            }

            .main-content {
                max-height: 800px;
            }

            /* Fix for Laptop L (1440px) screens */
            .hero-section {
                padding: 50px 35px;
            }

            .form-section {
                padding: 40px 30px;
            }

            .hero-title {
                font-size: 2.6rem;
            }

            .hero-subtitle {
                font-size: 1.2rem;
            }

            .form-title {
                font-size: 1.7rem;
            }

            .form-group {
                margin-bottom: 1.3rem;
            }

            .form-control {
                padding: 11px 15px;
            }

            .gender-label {
                padding: 9px 12px;
                font-size: 13px;
            }

            .btn-primary {
                padding: 13px 25px;
            }
        }

        /* XXL Screen (Extra Extra Large) - Extra Large Desktops */
        @media (min-width: 1400px) {
            .container.mt-4 {
                margin-top: 6rem !important;
                max-width: 1600px;
            }

            .main-content {
                max-height: 850px;
            }

            .hero-section {
                padding: 60px 50px;
            }

            .form-section {
                padding: 50px 45px;
            }

            .hero-title {
                font-size: 3.2rem;
            }

            .form-title {
                font-size: 2rem;
            }
        }

        /* Print styles */
        @media print {
            .hero-section {
                background: #f8f9fa !important;
                color: #000 !important;
            }

            .btn-primary {
                border: 2px solid #000 !important;
                background: white !important;
                color: #000 !important;
            }
        }
    </style>
</head>

<body>
    <!-- Navigation would be here -->
    <%- include('../layout/navbar.ejs') %>

    <div class="container mt-3 mb-3">
        <div class="main-content">
            <!-- Left Side - Hero Section -->
            <div class="hero-section">
                <div class="hero-content">
                    <h1 class="hero-title">Welcome to Product Store</h1>
                    <p class="hero-subtitle">Your one-stop shop for quality products, great prices, and exceptional
                        service.</p>

                    <ul class="feature-list">
                        <li><i class="fas fa-check-circle"></i> Verified seller with 5-star rating</li>
                        <li><i class="fas fa-truck"></i> Free delivery over $50</li>
                        <li><i class="fas fa-lock"></i> 100% secure checkout</li>
                        <li><i class="fas fa-smile"></i> Satisfaction guaranteed</li>
                    </ul>

                    <div class="testimonial">
                        <p class="testimonial-text">"I love shopping at Product Store! The website is easy to
                            navigate, the products are exactly as described, and the customer service team is
                            incredibly helpful."</p>
                        <p class="testimonial-author">- Sarah Chen, Happy Customer</p>
                    </div>
                </div>
            </div>

            <!-- Right Side - Registration Form -->
            <div class="form-section">
                <div class="form-content">
                    <div class="form-header">
                        <h2 class="form-title">Create an Account</h2>
                        <p class="form-subtitle">Join our community of happy shoppers</p>
                    </div>

                    <form action="/register/user" method="post" id="registrationForm" enctype="multipart/form-data">
                        <!-- Full Name -->
                        <div class="form-group">
                            <label for="name" class="form-label">Full Name</label>
                            <input type="text" class="form-control" id="name" name="name"
                                placeholder="Enter your full name" required>
                        </div>

                        <!-- Email -->
                        <div class="form-group">
                            <label for="email" class="form-label">Email Address</label>
                            <input type="email" class="form-control" id="email" name="email"
                                placeholder="Enter your email" required>
                        </div>

                        <!-- Gender -->
                        <div class="form-group">
                            <label class="form-label">Gender</label>
                            <div class="gender-group">
                                <div class="gender-option">
                                    <input type="radio" id="male" name="gender" value="male" required>
                                    <label for="male" class="gender-label">Male</label>
                                </div>
                                <div class="gender-option">
                                    <input type="radio" id="female" name="gender" value="female">
                                    <label for="female" class="gender-label">Female</label>
                                </div>
                                <div class="gender-option">
                                    <input type="radio" id="other" name="gender" value="other">
                                    <label for="other" class="gender-label">Other</label>
                                </div>
                            </div>
                        </div>

                        <!-- Date of Birth and City in one row -->
                        <div class="form-row">
                            <div class="form-group">
                                <label for="dob" class="form-label">Date of Birth</label>
                                <input type="date" class="form-control" id="dob" name="dob" required>
                            </div>
                            <div class="form-group">
                                <label for="city" class="form-label">City</label>
                                <input type="text" class="form-control" id="city" name="city" placeholder="Your city"
                                    required>
                            </div>
                        </div>

                        <!-- Phone Number -->
                        <div class="form-group">
                            <label for="phone" class="form-label">Phone Number</label>
                            <input type="tel" class="form-control" id="phone" name="phone"
                                placeholder="Enter your phone number" required>
                        </div>

                        <!-- Profile Image -->
                        <div class="form-group">
                            <label class="form-label">Profile Image</label>
                            <div class="file-upload-group">
                                <input type="file" class="custom-file-input" id="image" name="image" accept="image/*">
                                <label for="image" class="file-upload-label">
                                    <i class="fas fa-cloud-upload-alt"></i>
                                    <span>Choose profile picture</span>
                                    <small class="file-upload-text">Click to browse files</small>
                                </label>
                            </div>
                        </div>

                        <!-- Password -->
                        <div class="form-group">
                            <label for="password" class="form-label">Password</label>
                            <input type="password" class="form-control" id="password" name="password"
                                placeholder="Create a strong password" required>
                        </div>

                        <!-- Submit Button -->
                        <button type="submit" id="registerBtn" class="btn btn-primary">
                            <span class="btn-text">Create Account</span>
                            <div class="loading-spinner"></div>
                        </button>
                    </form>

                    <div class="login-link">
                        <p>Already have an account? <a href="/login">Login here</a></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <%- include('../layout/footer.ejs') %>

    <script src="https://cdn.jsdelivr.net/npm/jquery@3.5.1/dist/jquery.slim.min.js"
        integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"
        integrity="sha384-9/reFTGAW83EW2RDu2S0VKaIzap3H66lZH81PoYlFhbGU+6BZp6G7niu735Sk7lN"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.min.js"
        integrity="sha384-+sLIOodYLS7CIrQpBjl+C7nPvqq+FbNUBDunl/OZv93DB7Ln/533i8e/mZXLi/P+"
        crossorigin="anonymous"></script>

    <script>
        // Register button loading animation
        document.getElementById('registrationForm').addEventListener('submit', function (e) {
            const registerBtn = document.getElementById('registerBtn');
            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;

            // Basic validation
            if (!email || !password) {
                e.preventDefault();
                alert('Please fill in all required fields');
                return;
            }

            // Show loading state
            registerBtn.classList.add('btn-loading');

            // Optional: Auto remove loading state after 5 seconds (as fallback)
            setTimeout(() => {
                registerBtn.classList.remove('btn-loading');
            }, 5000);
        });

        // File input label update
        document.getElementById('image').addEventListener('change', function (e) {
            var fileName = e.target.files[0] ? e.target.files[0].name : 'Choose profile picture';
            var label = e.target.nextElementSibling;
            label.querySelector('span').textContent = fileName;
        });
    </script>

</body>

</html>